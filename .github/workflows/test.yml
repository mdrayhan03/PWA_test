name: Send Email Every 5min

on:
  schedule:
    - cron: "*/5 * * * *"   # Every 5 minutes
  workflow_dispatch:

jobs:
  send_email:
    runs-on: ubuntu-latest

    steps:
      - name: Install msmtp
        run: |
          sudo apt-get update
          sudo apt-get install -y msmtp mailutils

      - name: Configure SMTP
        run: |
          echo "defaults" > ~/.msmtprc
          echo "auth on" >> ~/.msmtprc
          echo "tls on" >> ~/.msmtprc
          echo "tls_trust_file /etc/ssl/certs/ca-certificates.crt" >> ~/.msmtprc
          echo "account default" >> ~/.msmtprc
          echo "host smtp.gmail.com" >> ~/.msmtprc
          echo "port 587" >> ~/.msmtprc
          echo "user ${{ secrets.EMAIL_USER }}" >> ~/.msmtprc
          echo "password ${{ secrets.EMAIL_PASS }}" >> ~/.msmtprc
          echo "from ${{ secrets.EMAIL_USER }}" >> ~/.msmtprc
          chmod 600 ~/.msmtprc

      # - name: Send Email
      #   run: |
      #     echo -e "This is an automated email sent every 5 minutes by GitHub Actions." \
      #     | msmtp -v ${{ secrets.ALERT_EMAIL }}
